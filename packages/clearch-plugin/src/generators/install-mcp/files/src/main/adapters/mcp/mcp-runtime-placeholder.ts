/**
 * Future: stdio/SSE MCP server using @modelcontextprotocol/sdk (or equivalent).
 * Register tools/resources/prompts and delegate to {@link McpToolHandler} implementations.
 */
export async function registerMcpRuntimePlaceholder(): Promise<void> {
  // No-op until an MCP runtime is installed.
}
