/**
 * Future: construct @grpc/grpc-js Server, TLS, interceptors, and register generated services.
 * Map incoming RPCs to {@link GrpcHandler} implementations from the presentation layer.
 */
export async function registerGrpcRuntimePlaceholder(): Promise<void> {
  // No-op until gRPC packages and protos are added.
}
